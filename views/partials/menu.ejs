 
 <style>
  #content {
        margin-left: 250px;
        transition: all 0.3s;
      }

      #content.expanded {
        margin-left: 70px;
      }

      .hero-section {
    position: fixed;
    width: 100%;
    height: calc(100vh - 70px);
    overflow: hidden;
}

.hero-image {
    width: 100%;
    height: 100%;
    object-fit: cover;
}

/* =========================================
   OVERLAY
========================================= */

.hero-overlay {

    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0, 0, 0, 0.45);
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    color: white;
    text-align: center;
    padding: 20px;
}

  /* =========================================
   TITRE ANIME
========================================= */

  .slide-title {

    font-size: 4rem;
    font-weight: bold;
    opacity: 0;
    transform: translateX(-200px);
    text-shadow: 0 0 20px rgba(255,255,255,0.8);
    animation: slideRight 4s ease-in-out infinite alternate;
  }

/* =========================================
   PARAGRAPHE ANIME
========================================= */

    .slide-text {

        font-size: 1.5rem;
        margin-top: 15px;
        opacity: 0;
        transform: translateX(-200px);
        animation: slideRight 4s ease-in-out infinite alternate;
        animation-delay: 0.5s;
    }

/* =========================================
   KEYFRAMES
========================================= */

    @keyframes slideRight {

        0% {
            opacity: 0;
            transform: translateX(-200px);
        }
        50% {
            opacity: 1;
            transform: translateX(0);
        }
        100% {
        opacity: 0.8;
        transform: translateX(150px);
       }
    }

    .hero-overlay h1 {
        font-size: 4rem;
        font-weight: bold;
    }

    .hero-overlay p {
        font-size: 1.5rem;
    }

    @media (max-width: 991.98px) {
      #content, #content.expanded {
        margin-left: 0;
      }
    }

    @media (max-width: 767.98px) {
      .hero-section {
        height: calc(100dvh - 70px);
      }
    }

 </style>

 <!-- 📊 CONTENT -->
     <div>
         <h2
          class=" content fw-bold text-center text-light bg-success p-3">
          Bienvenue dans le système de gestion pharmaceutique : PHARMA DU LOGONE
        </h2>
     </div>
     
    <div id="content" class="p-0">
          <div class="hero-section">
              <img src="/images/pharma-banniere.jfif" class="hero-image">

              <div class="hero-overlay">
                  <h1 class="text-center slide-text">  Pharma Management System  </h1>

                  <p class="text-center slide-text fw-bold"  style="color: yellow;"> 
                    Gestion moderne et intelligente de pharmacie </p>
              </div>
          </div>     
     </div>
